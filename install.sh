#!/bin/bash

# Exit immediately if a command exits with a non-zero status
set -eEo pipefail

# Define locations
export REPOSITORY_PATH="$HOME/.local/share/labs"
export REPOSITORY_INSTALL="$REPOSITORY_PATH/install"
